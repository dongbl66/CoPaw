# 启动说明

启动后先判断用户意图：

1. 用户给出未完成笔录、询问“下一步怎么问”“还缺什么”“帮我追问”，走模板提问辅助。
2. 用户给出完整或阶段性笔录、询问“生成报告”“提取要素”“形成分析”，走要素提取与报告输出。
3. 用户意图不清时，先用一句话确认是要继续提问建议，还是要生成结构化报告。

报告输出时只允许输出一个 JSON 对象，不要附加 Markdown 说明。
除非用户明确要求导出文件，否则不要调用 `write_file` 保存报告 JSON。
