import "@/business/marketing/manifest";
import "@/business/fae/manifest";

/**
 * 预加载内置业务模块。
 */
export function loadBuiltinBusinessModules(): void {
  // Import side effects register built-in business modules once at startup.
}
